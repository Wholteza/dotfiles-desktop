# Start section with >>> and end section with <<<
# Rows in between will be ensured in ~/.bashrc
>>>
# Alias definitions.
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi
<<<
>>>
# Bash definitions.
if [ -f ~/.bash_additions ]; then
    . ~/.bash_additions
fi
<<<